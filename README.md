# uE-OS

μE-OS is a hardware-agnostic, event-driven RTOS implementing the Active Object pattern. Featuring an O(1) software/hardware hybrid scheduler, deterministic static memory pools, and a unique TSM/FSM engine. Designed for zero-touch porting across STM32, ESP32, and POSIX, μE-OS ensures high-speed, reliable execution for critical IoT applications.
